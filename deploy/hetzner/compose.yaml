services:
  nutev:
    image: ${NUTEV_IMAGE:-nutev-local:latest}
    build:
      context: ../..
      dockerfile: deploy/hetzner/Dockerfile
      args:
        NUTEV_BUILD_COMMIT: ${NUTEV_BUILD_COMMIT:-unknown}
        NUTEV_BUILD_BRANCH: ${NUTEV_BUILD_BRANCH:-unknown}
        NUTEV_BUILD_TIME: ${NUTEV_BUILD_TIME:-unknown}
        NUTEV_VERSION: ${NUTEV_VERSION:-dev}
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NUTEV_OCR_LANG: ${NUTEV_OCR_LANG:-eng+por}
      NUTEV_BUILD_COMMIT: ${NUTEV_BUILD_COMMIT:-unknown}
      NUTEV_BUILD_BRANCH: ${NUTEV_BUILD_BRANCH:-unknown}
      NUTEV_BUILD_TIME: ${NUTEV_BUILD_TIME:-unknown}
      NUTEV_VERSION: ${NUTEV_VERSION:-dev}
      NUTEV_ENVIRONMENT: ${NUTEV_ENVIRONMENT:-production}
    expose:
      - "8765"
    ports:
      - "127.0.0.1:8765:8765"
    volumes:
      - nutev_output:/app/project_output_reference
    networks:
      - nutev_internal
    security_opt:
      - no-new-privileges:true

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    depends_on:
      nutev:
        condition: service_healthy
    env_file:
      - .env
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - nutev_internal
    security_opt:
      - no-new-privileges:true

networks:
  nutev_internal:
    driver: bridge

volumes:
  nutev_output:
  caddy_data:
  caddy_config:
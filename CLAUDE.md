# CLAUDE.md — NutEV

Before changing or analyzing this repository:

1. Read `AGENTS.md`.
2. Read `AI_CONTEXT.md`.
3. For Article 1, read `ARTICLE1_SEARCH_MASTER.md` and `config/nutev/article1_search_master_v1.json`.

The shared source of truth for ChatGPT/Codex and Claude is `AI_CONTEXT.md`; do not create a separate Claude-only scientific state.

If the live bundle exists, prefer `project_output_reference/agent_context/article1/CONTEXT_MANIFEST.json` plus `SEARCH_STATE.json` for mutable production state. Use `ARTICLE_SUMMARIES.jsonl` only as rank-blind navigation/context.

Never infer eligibility, quality, risk of bias, certainty, recommendation or PRISMA inclusion from Bank tier, route membership, machine profiles, retrieval status or agent summaries.

The Article 1 formal systematic-review search remains blocked until PRESS is PASS, GF-10 is explicitly authorized and provider-specific queries are version-frozen.
